Role: 你现在是一位具备深厚机器学习、概率统计理论基础的资深 AI 系统架构师。请协助我重构并优化一个基于多智能体（Master-Subagent）架构的中文学习测评系统。

Context:
目前我的系统通过 Master Agent 调度，结合知识图谱（DAG）和 HSK 词汇桶来评估用户的知识点（KC）掌握情况。目前遇到了评测流程缓慢、大模型调用开销大，以及缺乏严谨的置信度计算等问题。


Objective:
请基于现有的架构，帮我设计并实现以下三个维度的优化代码，重点提升系统的数学严谨性和运行效率。

Requirements:

1. 核心算法重构：引入基于 Beta 分布的贝叶斯知识追踪（BKT）

    请设计一个 KnowledgeState 类来管理每个 KC 节点的 α 和 β 参数。

    实现期望计算公式 E(X)=α+βα​ 作为掌握概率，以及方差映射公式以计算置信度。

    重写状态更新逻辑：结合耗时分析，设计一个基于时间的衰减惩罚系数 γ。当实际耗时 Tactual​ 远大于预期耗时 Texpected​ 时，即便用户答对，也只增加 γ 的证据权重，而非完整的 1。

2. 图谱逻辑优化：DAG 置信度反向传播

    在知识图谱的更新逻辑中，添加反向传播机制。

    当某个高阶 KC 节点（如“把字句”）的置信度达到设定阈值且掌握概率较高时，根据 DAG 的边权重，自动按比例推高其所有前置依赖节点（如“基本主谓宾结构”）的 α 值，从而减少对基础语法的冗余测试。


Output Format:

    架构设计说明： 简述重构后的模块交互数据流。

    核心代码实现： 使用 Python 提供 KnowledgeState、置信度计算以及 DAG 更新的核心逻辑代码，请务必包含详细的数学逻辑注释。

    集成建议： 指出这段新代码该如何安全地接入我现有的 Master-Subagent 工作流中。